{
  "name": "where-is-my-child",
  "version": "0.1.0",
  "private": true,
  "description": "find your child",
  "dependencies": {
    "joi": "^17.11.0"
  },
  "scripts": {
    "build": "npm install",
    "dev": "npx wrangler pages dev views --d1=DB",
    "bootstrap-db": "npx wrangler d1 execute where-is-my-child --local --file bootstrap.sql"
  },
  "devDependencies": {
    "wrangler": "^3.11.0"
  }
}
